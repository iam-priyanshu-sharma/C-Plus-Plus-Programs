#include <iostream>

using namespace std;

int main()
{
    float a, b, c, d, sum;
    cout << "Enter 4 float values:";
    cin >> a >> b >> c >> d;
    sum = a + b + c + d;
    cout << "\nSum=" << sum;
    return 0;
}